没有我的同意，不要擅自改动代码
SVN 提交信息用 -F 消息文件，不直接传中文参数
提交后用 svn log --xml 校验
Windows上的文件保持 CRLF

如果是一些工作流，而不是分析或者改代码，一直到工作到完成不要停止

如果有提及自定义指令之类的，需要再看一眼全局的自定义指令

git 如果是自己的仓库，默认提交后直接推送

创建文章模板时只创建文件和目录，不提交、不推送；用户写完内容后再提交推送
